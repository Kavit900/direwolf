<%= form_for(@apply) do |f| %>
  <% if @apply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apply.errors.count, "error") %> prohibited this apply from being saved:</h2>

      <ul>
      <% @apply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cover %><br>
    <%= f.text_area :cover, :placeholder => "Cover Letter Here" %>
  </div>
    <%= f.hidden_field :job_id, :value => params[:job_id] %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
